# Echtra articles

Each `.html` file is an independent article discovered automatically by `app.js`.

The source manuscript is intentionally preserved at article level rather than collapsed into a single page.