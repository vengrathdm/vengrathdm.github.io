# Echtra midst the Sidhe

Campaign codex structure. Articles live in `articles/` and are discovered automatically by `app.js` through the GitHub Contents API.

The current site shell is intentionally separate from Heartwell so the campaign can have its own typography, palette, and content taxonomy while retaining the same navigational model.
