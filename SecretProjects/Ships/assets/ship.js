const file=document.body.dataset.model;const raceParam=new URLSearchParams(location.search).get('race');const s=SHIPS.find(x=>x.file===file&&(!raceParam||x.raceId===raceParam));const a=document.getElementById('ship');const n=x=>Number(x).toLocaleString('en-US');
if(!s){a.innerHTML='<p>MODEL NIEZNANY.</p>'}else{
 document.body.classList.add('race-'+s.raceId.toLowerCase());document.body.dataset.race=s.raceId;
 const css=document.createElement('link');css.rel='stylesheet';css.href=`../assets/race-${s.raceId.toLowerCase().replace('race','')==='race1'?'terran':s.raceId.toLowerCase().replace('race','')==='race2'?'turian':s.raceId.toLowerCase().replace('race','')==='race3'?'aasari':s.raceId.toLowerCase().replace('race','')==='race4'?'quarian':'hanar'}.css`;document.head.appendChild(css);
 if(s.raceId==='Race5'){
   const seg=(value,max=100)=>Math.max(0,Math.min(100,Math.round(value/max*100)));
   const meter=(value,max=100)=>{const on=Math.max(1,Math.min(30,Math.round(seg(value,max)/100*30)));return Array.from({length:30},(_,i)=>`<i class="${i<on?'on':''}"></i>`).join('')};
   const signal=Math.max(12,Math.min(99,Math.round(72+s.generation*5-(s.slots[2]*2))));
   const threat=Math.max(18,Math.min(99,Math.round(100-(s.h/(s.h+s.s))*45+s.w/(s.h+s.w)*35)));
   a.innerHTML=`
   <div class="hanar-monitor"><div class="hanar-glass"><div class="hanar-terminal"><div class="hanar-noise"></div><div class="hanar-corners"></div>
     <header class="hanar-header"><div class="hanar-system">[ FLOTTA HANAR // ARCHIWUM TECHNICZNE // ŁĄCZE ZABEZPIECZONE ]</div><div class="hanar-title">VESSEL RECORD // ${s.name.toUpperCase()}</div><div class="hanar-subtitle">NAVAL CONSTRUCTION &amp; SERVICE DATABASE // NODE ${String(s.generation).padStart(2,'0')}-${String(s.slots[0]).padStart(2,'0')}</div><div class="hanar-status"><span class="hanar-live"></span> LINK ACTIVE<br>CLASS: <b>${s.class.toUpperCase()}</b><br>RACE: <b>HANAR</b></div></header>
     <main class="hanar-main">
       <aside class="hanar-identity"><div class="hanar-emblem"><svg viewBox="0 0 160 200"><path d="M80 7 L148 35 L132 124 L80 190 L28 124 L12 35 Z" fill="none" stroke="currentColor" stroke-width="4"/><path d="M80 25 L125 44 L114 115 L80 162 L46 115 L35 44 Z" fill="none" stroke="currentColor" stroke-width="2"/><path d="M80 47 L86 73 L112 80 L86 87 L80 113 L74 87 L48 80 L74 73 Z" fill="currentColor"/><circle cx="80" cy="80" r="8" fill="#050303"/></svg></div><div class="hanar-caption">VESSEL IDENTIFICATION MATRIX</div><div class="hanar-classification"><div><span>MODEL</span><b>${s.name.toUpperCase()}</b></div><div><span>CLASS</span><b>${s.class.toUpperCase()}</b></div><div><span>GENERATION</span><b>${s.generation}. GEN</b></div><div><span>REGISTRY</span><b>HN-${String(s.generation).padStart(2,'0')}-${String(s.length).padStart(4,'0')}</b></div><div><span>STATUS</span><b class="hot">OPERATIONAL</b></div><div><span>AUTHORITY</span><b>NAVAL ARCHIVE</b></div></div></aside>
       <section class="hanar-content"><article class="hanar-warning"><div class="hanar-warning-head">[!] TECHNICAL RECORD // PRIORITY ${s.generation>=3?'HIGH':'NORMAL'}</div><h1>${s.name.toUpperCase()} — <span>ACTIVE VESSEL</span></h1><div class="hanar-copy">UNIT REGISTERED AS <strong>${s.class.toUpperCase()}</strong>. ARCHIVE DATA CONFIRMED. HULL INTEGRITY, SHIELD ARRAY AND WEAPON SYSTEMS ARE WITHIN RECORDED PARAMETERS. THIS TERMINAL EXPOSES SERVICE DATA FOR AUTHORIZED CREW.</div><div class="hanar-threat"><div><span>COMBAT THREAT INDEX</span><b>${threat}%</b></div><div class="hanar-meter">${meter(threat,100)}</div></div></article>
         <div class="hanar-data-grid">
          <div class="hanar-data"><div>HULL INTEGRITY</div><strong>${n(s.h)} MJ</strong><span>${meter(s.h,Math.max(s.h,80000))}</span></div>
          <div class="hanar-data"><div>SHIELD CAPACITY</div><strong>${n(s.s)} MJ</strong><span>${meter(s.s,Math.max(s.s,15000))}</span></div>
          <div class="hanar-data"><div>WEAPON OUTPUT</div><strong>${n(s.w)} MJ/min</strong><span>${meter(s.w,Math.max(s.w,35000))}</span></div>
          <div class="hanar-data"><div>GENERATOR</div><strong>${n(s.generator)} MW</strong><span>${meter(s.generator,Math.max(s.generator,40000))}</span></div>
          <div class="hanar-data"><div>CAPACITOR BANK</div><strong>${n(s.capacitor)} MJ</strong><span>${meter(s.capacitor,Math.max(s.capacitor,40000))}</span></div>
          <div class="hanar-data"><div>SHIELD RECHARGE</div><strong>${n(s.r)} MJ/min</strong><span>${meter(s.r,Math.max(s.r,1500))}</span></div>
         </div>
       </section>
     </main>
     <section class="hanar-transcript"><div class="hanar-tabs"><span class="active">VESSEL RECORD</span><span>ENGINEERING</span><span>ARMAMENT</span><span>CREW LOG</span></div><div class="hanar-report"><div class="hanar-report-label">SHIP DATA<br>CREW CAPACITY<br>CARGO<br>LENGTH<br>MODULE HARDPOINTS<br>MARKET VALUE<br>SERVICE STATE</div><div class="hanar-report-data"><strong>ARCHIVE ENTRY // ${s.name.toUpperCase()}</strong><br>CREW CAPACITY . . . <strong>${n(s.crew)}</strong><br>CARGO . . . <strong>${s.cargo} t</strong><br>LENGTH . . . <strong>${n(s.length)} m</strong><br>HARDPOINTS . . . <strong>X:${s.slots[0]} / Y:${s.slots[1]} / Z:${s.slots[2]}</strong><br>MARKET VALUE . . . <strong>${n(s.price)} KCr</strong><br>SERVICE STATE . . . <strong class="hot">${signal>=70?'READY // VERIFIED':'LIMITED // INSPECTION REQUIRED'}</strong></div></div></section>
     <footer class="hanar-footer"><span class="online">●</span> HANAR NAVAL ARCHIVE // CONNECTION SECURE <span class="right">NODE HN-${s.generation} // SIGNAL ${signal}% &nbsp;|&nbsp; <a href="../index.html">RETURN TO CATALOGUE</a></span></footer>
   </div></div><div class="hanar-brand">SYSTECH INDUSTRIES <span>ARCHIVE TERMINAL // R-9H</span></div></div>`;
 }else{
   const metrics=[['KADŁUB',s.h,'MJ'],['OSŁONY',s.s,'MJ'],['REGENERACJA OSŁON',s.r,'MJ/min'],['SIŁA OGNIA',s.w,'MJ/min'],['ZAŁOGA',s.crew,''],['ŁADOWNOŚĆ',s.cargo,'t'],['DŁUGOŚĆ',s.length,'m'],['BATERIA KONDENSATORÓW',s.capacitor,'MJ'],['GENERATOR MOCY',s.generator,'MW']];
   a.innerHTML=`<a class="back" href="../index.html">← KATALOG FLOTY</a><header><div class="eyebrow">[ DOKUMENTACJA // INTERFEJS ${raceName(s.raceId).toUpperCase()} ]</div><h1>${s.name}</h1><h2>${s.class} — ${s.name}</h2><p>${s.subclass}</p><p class="identity">${raceName(s.raceId)} · <b>${s.generation}. generacja</b></p><strong class="price">${n(s.price)} <small>KCr</small></strong></header><section class="ship-visual" aria-label="Wizualizacja statku"><div class="orbit orbit-a"></div><div class="orbit orbit-b"></div><div class="ship-core"><span></span></div><div class="scan-label">${s.class.toUpperCase()} // ${s.generation}. GEN</div></section><section class="grid">${metrics.map(x=>`<div class="card"><b>${x[0]}</b><strong>${x[1]} ${x[2]}</strong><i></i></div>`).join('')}</section><section class="panel"><h2>GNIAZDA MODUŁOWE</h2><div class="slots"><span>X — UZBROJENIE × ${s.slots[0]}</span><span>Y — OSŁONY × ${s.slots[1]}</span><span>Z — WSPARCIE × ${s.slots[2]}</span></div></section><section class="panel"><h2>FILOZOFIA KONSTRUKCYJNA // ${raceName(s.raceId).toUpperCase()}</h2><p>${RACE_PHILOSOPHY[s.raceId]}</p></section>`;
 }
}