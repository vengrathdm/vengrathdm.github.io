const file=document.body.dataset.model,s=SHIPS.find(x=>x.file===file),a=document.getElementById('ship');const n=x=>Number(x).toLocaleString('en-US');if(!s)a.innerHTML='<p>MODEL NIEZNANY.</p>';else a.innerHTML=`<a class="back" href="../index.html">← KATALOG FLOTY</a><header><div class="eyebrow">DOKUMENTACJA MODELU</div><h1>${s.name}</h1><h2>${s.class} — ${s.name}</h2><p>${s.subclass}</p><p class="identity">${raceName(s.raceId)} · ${s.generation}. generacja</p><strong class="price">${n(s.price)} KCr</strong></header><section class="grid">${[['KADŁUB',s.h+' MJ'],['OSŁONY',s.s+' MJ'],['REGENERACJA OSŁON',s.r+' MJ/min'],['SIŁA OGNIA',s.w+' MJ/min'],['ZAŁOGA',s.crew],['ŁADOWNOŚĆ',s.cargo+' t'],['DŁUGOŚĆ',s.length+' m'],['BATERIA KONDENSATORÓW',s.capacitor+' MJ'],['GENERATOR MOCY',s.generator+' MW']].map(x=>`<div class="card"><b>${x[0]}</b><strong>${x[1]}</strong></div>`).join('')}</section><section class="panel"><h2>GNIAZDA</h2><p>X — UZBROJENIE × ${s.slots[0]} · Y — OSŁONY × ${s.slots[1]} · Z — WSPARCIE × ${s.slots[2]}</p></section><section class="panel"><h2>FILOZOFIA RASY</h2><p>${RACE_PHILOSOPHY[s.raceId]}</p></section>`;