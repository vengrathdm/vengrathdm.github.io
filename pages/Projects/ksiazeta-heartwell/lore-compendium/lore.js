const root=document.querySelector('#lore-index'),status=document.querySelector('#lore-status'),search=document.querySelector('#lore-search');
fetch('./lore.json',{cache:'no-store'}).then(r=>r.ok?r.json():Promise.reject(new Error(`HTTP ${r.status}`))).then(data=>{const render=query=>{const needle=query.trim().toLowerCase();root.innerHTML=data.categories.map(category=>{const entries=data.entries.filter(([id,title])=>id===category.id&&(!needle||title.toLowerCase().includes(needle)));if(!entries.length)return '';return `<section class="lore-category"><p class="eyebrow">${escapeHtml(category.label)}</p><div class="lore-links">${entries.map(([,title,slug])=>`<a href="${encodeURIComponent(slug)}/"><span>${escapeHtml(title)}</span><b>↗</b></a>`).join('')}</div></section>`}).join('')||'<p class="project-empty">Brak wpisów dla tego wyszukiwania.</p>';status.textContent=`${data.entries.filter(([,title])=>!needle||title.toLowerCase().includes(needle)).length} WPISÓW`;};render('');search?.addEventListener('input',e=>render(e.target.value));}).catch(e=>{console.error(e);status.textContent='BŁĄD ŁADOWANIA';root.innerHTML='<p class="project-empty">Nie udało się załadować indeksu lore.</p>';});
function escapeHtml(value){return String(value??'').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]));}