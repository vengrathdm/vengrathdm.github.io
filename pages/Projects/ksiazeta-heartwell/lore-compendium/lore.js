const root=document.querySelector('#lore-index'),status=document.querySelector('#lore-status'),search=document.querySelector('#lore-search');
const articleSlug=new URLSearchParams(location.search).get('article');
if(articleSlug){loadArticle(articleSlug);}else{loadIndex();}
function loadIndex(){fetch('./lore.json',{cache:'no-store'}).then(r=>r.ok?r.json():Promise.reject(new Error(`HTTP ${r.status}`))).then(data=>{const render=query=>{const needle=query.trim().toLowerCase();root.innerHTML=data.categories.map(category=>{const entries=data.entries.filter(([id,title])=>id===category.id&&(!needle||title.toLowerCase().includes(needle)));if(!entries.length)return '';return `<section class="lore-category"><p class="eyebrow">${escapeHtml(category.label)}</p><div class="lore-links">${entries.map(([,title,slug])=>`<a href="?article=${encodeURIComponent(slug)}"><span>${escapeHtml(title)}</span><b>↗</b></a>`).join('')}</div></section>`}).join('')||'<p class="project-empty">Brak wpisów dla tego wyszukiwania.</p>';status.textContent=`${data.entries.filter(([,title])=>!needle||title.toLowerCase().includes(needle)).length} WPISÓW`;};render('');search?.addEventListener('input',e=>render(e.target.value));}).catch(showError);}
function loadArticle(slug){if(search)search.closest('.lore-tools')?.remove();fetch('./articles.json',{cache:'no-store'}).then(r=>r.ok?r.json():Promise.reject(new Error(`HTTP ${r.status}`))).then(data=>{const a=data[slug];if(!a)throw new Error('Article not found');document.title=`${a.title} — Heartwell — Vengrath`;root.innerHTML=`<article class="project-copy" style="max-width:900px"><p><a href="./">← Wróć do kompendium</a></p><h1>${escapeHtml(a.title)}</h1><p class="project-lead">${escapeHtml(a.lead)}</p>${a.body.map(p=>`<p>${escapeHtml(p)}</p>`).join('')}</article>`;status.textContent='HEARTWELL';}).catch(showError);}
function showError(e){console.error(e);if(status)status.textContent='BŁĄD ŁADOWANIA';if(root)root.innerHTML='<p class="project-empty">Nie udało się załadować wpisu.</p>';}
function escapeHtml(value){return String(value??'').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]));}