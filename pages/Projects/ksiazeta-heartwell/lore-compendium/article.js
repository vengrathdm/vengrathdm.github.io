const root=document.querySelector('#article-content');
fetch('../articles.json',{cache:'no-store'}).then(r=>r.ok?r.json():Promise.reject(new Error(`HTTP ${r.status}`))).then(data=>{const slug=location.pathname.split('/').filter(Boolean).pop();const article=data[slug];if(!article)throw new Error('Article not found');document.title=`${article.title} — Heartwell — Vengrath`;root.innerHTML=`<section class="project-hero"><h1>${escapeHtml(article.title)}</h1><p class="project-lead">${escapeHtml(article.lead)}</p></section><section class="project-body"><article class="project-copy">${article.body.map(p=>`<p>${escapeHtml(p)}</p>`).join('')}</article><aside class="project-aside"><a class="project-back" href="../">← Wróć do kompendium</a></aside></section>`;}).catch(e=>{console.error(e);root.innerHTML='<section class="project-body"><article class="project-copy"><h2>Nie znaleziono wpisu.</h2><p>Ten artykuł nie istnieje w kompendium Heartwell.</p></article></section>';});
function escapeHtml(value){return String(value??'').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]));}