(()=>{const root=document.querySelector('[data-canvas]');if(!root)return;const viewport=root.querySelector('.canvas-viewport'),track=root.querySelector('.canvas-track'),slides=[...root.querySelectorAll('.canvas-slide')],nav=[...root.querySelectorAll('[data-canvas-to]')],prev=root.querySelector('[data-canvas-prev]'),next=root.querySelector('[data-canvas-next]');let index=0,target=0,current=0,dragging=false,startX=0,startCurrent=0,activePointer=null;const clamp=(n,a,b)=>Math.max(a,Math.min(b,n));const max=()=>Math.max(0,slides.length-1);function setIndex(i){index=clamp(i,0,max());target=index;nav.forEach((b,n)=>{const active=n===index;b.classList.toggle('is-active',active);if(active)b.setAttribute('aria-current','true');else b.removeAttribute('aria-current');});prev.disabled=index===0;next.disabled=index===max()}function render(){current+=(target-current)*.11;if(Math.abs(target-current)<.002)current=target;track.style.transform=`translate3d(${-current*100}%,0,0)`;requestAnimationFrame(render)}function nearest(){setIndex(Math.round(current))}function endDrag(){if(!dragging)return;dragging=false;viewport.classList.remove('is-dragging');if(activePointer!==null)viewport.releasePointerCapture?.(activePointer);nearest()}viewport.addEventListener('pointerdown',e=>{if(e.pointerType==='mouse'&&e.button!==0)return;dragging=true;activePointer=e.pointerId;startX=e.clientX;startCurrent=current;viewport.classList.add('is-dragging');viewport.setPointerCapture?.(e.pointerId)});viewport.addEventListener('pointermove',e=>{if(!dragging)return;const dx=e.clientX-startX,width=viewport.clientWidth||1;current=clamp(startCurrent-dx/width,0,max());target=current;e.preventDefault()});viewport.addEventListener('pointerup',endDrag);viewport.addEventListener('pointercancel',endDrag);viewport.addEventListener('click',e=>{if(dragging||Math.abs(current-index)>.08){e.preventDefault();e.stopPropagation()}});nav.forEach(b=>b.addEventListener('click',()=>setIndex(Number(b.dataset.canvasTo))));prev?.addEventListener('click',()=>setIndex(index-1));next?.addEventListener('click',()=>setIndex(index+1));root.addEventListener('keydown',e=>{if(e.key==='ArrowLeft'){e.preventDefault();setIndex(index-1)}if(e.key==='ArrowRight'){e.preventDefault();setIndex(index+1)}});root.setAttribute('tabindex','0');setIndex(0);render()})();